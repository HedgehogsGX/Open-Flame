#!/bin/sh
set -eu

# Reject trailing whitespace and conflict markers in the staged diff. Tests are
# maintained like ordinary source; see AGENTS.md for the evidence rule that
# replaced the old commit-scope gate.
exec git diff --cached --check
