# v0.24.2 下载器与上传器集成、控件修复和测试交付

日期：2026-09-05。首批平台为 Bilibili、抖音、视频号；其他平台后续维护。下载 Schema 11、上传独立 Schema 1 保持。

本页前半部分保留首次提交及旧测试包的验收；当前推送构建与新增修复见末节“推送前再次调试”。

## 本轮范围

开发与调试完成后提交代码，并提供 [外部上传测试计划](../docs/UPLOADER_TEST_PLAN.md)。实际平台媒体传输、投稿及后台结果核对由用户安排的测试员完成。本轮不执行真实上传，也不把外部测试完成作为本地开发交付的前提。

## 已有真实账号与控件证据

用户完成 Bilibili 扫码后，页面显示登录成功；独立“检查登录态”操作返回 ready。使用已有 5 秒合成视频实际检查本地文件选择、导入、标题/账号/视频/分区/类型/转载来源/标签校验、草稿预览、取消与重新创建。两个测试任务均已取消，没有确认上传；账号状态保持 ready。

这些操作不证明 Bilibili 的投稿权限或媒体传输已成功。抖音、视频号仅已有真实二维码获取证据，尚未完成用户真实扫码认证。前一轮二维码实现与取码证据见 [0.24.1](iteration-0.24.1-qr-login-evidence.md)。

## 修复与集成验证

修复草稿详情手动折叠后被自动刷新重新展开：按任务 ID 保留实际展开状态，新草稿默认展开，取消、终态、列表重排和新建重试任务彼此独立。新增回归同时检查账号选择、视频选择与二维码状态不被改动。

对下载成品接入上传的验证使用真实下载登记、资产存储和 API 解析，不以替换成品查询函数代替完整关联。联网下载执行器与平台上传执行器均为离线替身；本轮没有请求第三方媒体下载或上传。

新增完整登记链路集成测试 **10 项**：原批次及 duplicate 批次成品入口、真实 owner/asset/artifact 关联、独立副本、原件与下载数据库不变；非 ready 登记、登记 hash 冲突、文件缺失、大小及同大小内容变化均拒绝导入。与既有上传 API/下载成品 UI 的组合回归 **41 项通过**。详情状态修复后的上传前端组 **12 项通过**。

完整回归 **1859 passed, 8 skipped in 202.89s**。8 项跳过均为 Windows 不具备的指定 POSIX 能力。

在独立端口 18842，实际浏览器从最近下载批次打开 ready 成品，点击“用于上传”并显式导入；使用之前已验证的 5 秒、56,111 字节视频，SHA-256 与原登记一致。真实 UI 创建 Bilibili/抖音投稿模式和视频号平台草稿模式共3份本地草稿，验证标题、简介、标签、分区、类型和模式，再逐项取消并返回下载列表，原 ready 成品仍可访问。Bilibili 详情手动折叠后跨自动轮询及手动刷新保持，其他任务展开状态和3个账号选择独立保留；无水平溢出，浏览器 warning/error 记录为空。

该 fixture 的平台 backend 禁止所有登录、检查与上传。持久的 HTTP 事件仅记录一次下载成品导入、两次本地草稿创建和三次取消；确认接口请求为0，账号操作为0。隔离服务随后正常请求关闭并释放端口。此链路验证本地集成，不是真实下载或真实上传证明。原始证据位于 ignored `validation/local/downloader-uploader-20260905/`。

普通上传预览18839在确认账号操作和上传任务均空闲后重启到本轮构建，Bilibili保持ready，两条先前测试记录保持canceled，未新增上传操作。测试计划读取构建身份的实际API命令已在该实例验证。

当前发行清单为 **294** 个文件；8份主要文档共 **110** 个本地链接检查通过，whitespace检查通过。精确包身份为 `0.24.2+build.sha256.476055791c765b08d1e96b7819240b3bf495dcbda683532fdb58cf5c2327dbfd`。新增Git属性保留上传runtime-lock.json的原始字节，避免换行转换使安装清单的锁文件hash失效；提交时再次核对Git对象与工作树的包字节一致。

本地发行输出为 ignored `dist/Open-Flame-0.24.2-test-candidate-r2/`；构建、独立安装及提交绑定结果由归档外验收报告记录。源码归档不包含账号、数据库、媒体、运行日志或工具环境。

## 交付与后续测试

外部测试员必须分别核对 Bilibili、抖音、视频号；视频号保存平台草稿也会传输视频，需与本地草稿分开验证。测试计划包含两个输入入口、账号检查、平台结果核对、取消/中断、unknown 防重复提交及结果回报模板。

本机开发回归不能替代真实平台测试；Windows 的 POSIX 跳过项也不能证明 Linux / Docker / NAS 支持。Git 提交、源码制品与公开 GitHub Release 是不同交付动作，具体提交与制品摘要以 Git 和归档外报告为准。

## 推送前再次调试（2026-09-05）

用户要求再次 debug 后直接推送代码与测试说明，继续保持“不真实上传媒体”的边界。基线提交 `cab5526` 的既有全量测试为 **1859 passed, 8 skipped in 205.24s**；独立代码检查和实际浏览器仍发现两项原测试未覆盖的交互缺陷。

| 场景 | 修复前实际表现 | 修复后实际表现 |
| --- | --- | --- |
| 清空“已导入的视频”后轮询或点击“刷新状态” | 自动选回第一项；所选来源离开列表时也会静默换源 | 保持无选择、清空 SHA-256 信息，创建草稿提示先选视频；首次载入默认与主动导入后选中新来源仍可用 |
| 取消 A → 重建 B → 取消 B → 再点 A 重建 | 服务幂等返回已有 B，但页面误报新草稿已创建 | 按返回状态提示“未创建新草稿：已有后继任务为已取消”，定位 B，任务数量不增加；后继为本地草稿时提示待核对 |

新增 8 项回归在旧源码上 **7 failed / 1 passed**，修复后上传前端组 **20 passed**；包含清空、所选来源缺失、再次导入、后继为 draft/canceled/running/submitted/draft_saved 的提示与定位。服务重建幂等行为和逐项确认门槛保持，测试未调用确认接口。最终完整回归为 **1867 passed, 8 skipped in 204.46s**，跳过原因仍为本机不具备的指定 POSIX 能力。

隔离浏览器端口 18843 从零创建 fixture，先复现两项问题，再重启至修复构建验证：下载成品导入、本地文件导入、清空来源、必填阻止、三平台本地草稿、各任务详情跨轮询/手动刷新保持、重建状态与定位、取消均符合预期；没有水平溢出，浏览器 warning/error 为空。合计 7 个本地任务最终均 canceled、2 个来源文件 SHA-256 与既有 5 秒合成视频一致。持久 HTTP 事件为下载成品导入 1 次、本地导入 1 次、草稿创建请求 4 次、取消 7 次、重建 3 次，确认上传请求 **0**，账号操作 **0**。fixture 的平台后端始终禁止真实登录、检查及上传。

普通预览 18839 在确认账号操作和上传任务空闲后重启至修复构建，浏览器实测清空视频后刷新保持空；Bilibili 账号仍为 ready、原有 2 个任务仍 canceled、原有 2 个账号操作仍 ready，没有新增任务或账号操作。本轮不重新扫码，不验证真实平台传输、投稿或审核。

最终包身份为 `0.24.2+build.sha256.a15efcc694d4123724e5c0e9983cc16235a704dc9d9be576a315ff926c8dc3f6`。294 文件发行清单与源码隐私检查通过，6 份主要文档共 88 个本地文件链接通过。原始证据位于 ignored `validation/local/uploader-push-debug-20260905/`；不会加入 Git。此前 `test-candidate-r2` 的源码 ZIP/wheel 安装报告仍属于旧构建，本次未重建该包或新建 GitHub Release。

交付分支为 `codex/uploader-first-platforms`，推送内容包含代码、回归测试、本记录与更新的[测试说明](../docs/UPLOADER_TEST_PLAN.md)。测试员从该分支获取完整源码，先按快速开始检查本地控件，再自行决定三平台真实登录与投稿，按模板分别回报。当前测试说明增加分支下载/克隆入口及 SRC-04、DRAFT-05、DRAFT-06 三项控件回归。
